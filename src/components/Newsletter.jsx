import { styles, GetStarted } from '../ui';

const Newsletter = () => {
  return (
    <section className={`${styles.flexCenter}`}>
      <GetStarted />
    </section>
  );
};

export default Newsletter;
